#include <stdio.h>

int main() {
    int qtd, num, cont1, cont2;
    printf("Digite a quantidade de numeros\n");
    scanf("%d", &qtd);
    for(int i=0; i<qtd;i++){
        printf("Insira o numero\n");
        scanf("%d", &num);
        if(num>=10 && num<=20){
            cont1++;
        }else{
            cont2++;
        }
    }
    printf("in %d\n", cont1);
    printf("out %d", cont2);
    return 0;
}
